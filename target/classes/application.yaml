server:
  port: 8080

spring:
  application:
      name: Focused Fitness Application
  datasource:
    url: jdbc:mysql://localhost:3306/gym_attendance
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
